Snapshot of js/pose-rules.js at commit 5f92a3c — the rep engine BEFORE the
counting fixes. Kept so ml/eval/rep_compare.py and the synthetic stress
harness can measure before/after on identical input. Never loaded by the app.
